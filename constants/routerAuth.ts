/**
 * auth : 是否需要登入狀態
 * loginFlow : (ex: true => 登入狀態下到達此路由，將直接導回首頁)
 * disable : 頁面是否暫時禁止
 * routerReplace : 覆寫預設頁面 (router name)
 */
export default {
    // ['privacy']: {
    //     routerReplace: 'privacy-terms-of-service',
    // },
}
